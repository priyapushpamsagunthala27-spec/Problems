int hammingWeight(int n) {
    unsigned int num = (unsigned int)n;  // handle bits properly
    int count = 0;

    for (int i = 0; i < 32; i++) {
        if (num & 1)
            count++;
        num >>= 1;
    }

    return count;
}
